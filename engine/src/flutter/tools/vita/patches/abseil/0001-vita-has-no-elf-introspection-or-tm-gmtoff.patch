diff --git a/absl/base/internal/low_level_alloc.cc b/absl/base/internal/low_level_alloc.cc
index a5bd71d..bb2fa00 100644
--- a/absl/base/internal/low_level_alloc.cc
+++ b/absl/base/internal/low_level_alloc.cc
@@ -26,7 +26,11 @@
 
 #include "absl/base/call_once.h"
 #include "absl/base/config.h"
+#if defined(__vita__)
+#include <malloc.h>  // memalign
+#else
 #include "absl/base/internal/direct_mmap.h"
+#endif
 #include "absl/base/internal/scheduling_mode.h"
 #include "absl/base/macros.h"
 #include "absl/base/thread_annotations.h"
@@ -39,7 +43,9 @@
 #ifndef _WIN32
 #include <pthread.h>
 #include <signal.h>
+#if !defined(__vita__)
 #include <sys/mman.h>
+#endif
 #include <unistd.h>
 #else
 #include <windows.h>
@@ -436,7 +442,14 @@ bool LowLevelAlloc::DeleteArena(Arena *arena) {
     ABSL_RAW_CHECK(munmap_result != 0,
                    "LowLevelAlloc::DeleteArena: VitualFree failed");
 #else
-#ifndef ABSL_LOW_LEVEL_ALLOC_ASYNC_SIGNAL_SAFE_MISSING
+#if defined(__vita__)
+    // The counterpart of the memalign above. The pointer is the one memalign
+    // returned -- whole regions are allocated and whole regions freed -- so
+    // free() is correct and the size is unused.
+    (void)size;
+    ::free(region);
+    munmap_result = 0;
+#elif !defined(ABSL_LOW_LEVEL_ALLOC_ASYNC_SIGNAL_SAFE_MISSING)
     if ((arena->flags & LowLevelAlloc::kAsyncSignalSafe) == 0) {
       munmap_result = munmap(region, size);
     } else {
@@ -444,7 +457,7 @@ bool LowLevelAlloc::DeleteArena(Arena *arena) {
     }
 #else
     munmap_result = munmap(region, size);
-#endif  // ABSL_LOW_LEVEL_ALLOC_ASYNC_SIGNAL_SAFE_MISSING
+#endif  // defined(__vita__)
     if (munmap_result != 0) {
       ABSL_RAW_LOG(FATAL, "LowLevelAlloc::DeleteArena: munmap failed: %d",
                    errno);
@@ -583,6 +596,25 @@ static void *DoAllocWithArena(size_t request, LowLevelAlloc::Arena *arena) {
                                MEM_RESERVE | MEM_COMMIT, PAGE_READWRITE);
       ABSL_RAW_CHECK(new_pages != nullptr, "VirtualAlloc failed");
 #else
+#if defined(__vita__)
+      // Page-aligned so the arithmetic below, which assumes whole pages,
+      // continues to hold. memalign rather than mmap; see low_level_alloc.h.
+      new_pages = memalign(arena->pagesize, new_pages_size);
+      if (new_pages == nullptr) {
+        ABSL_RAW_LOG(FATAL, "memalign error: %d", errno);
+      }
+      // **Zero it.** This is not tidiness, it is the whole difference between
+      // mmap and memalign: MAP_ANONYMOUS pages arrive zero-filled and heap
+      // memory arrives holding whatever was there before. Abseil's callers --
+      // CreateThreadIdentity in particular -- take fresh arena memory and use
+      // fields without initialising every one, which is correct against mmap
+      // and silently wrong here.
+      //
+      // The symptom was a data abort in pte_osAtomicExchange with a pointer of
+      // 0x6f742d65, which is ASCII: a synchronisation object holding recycled
+      // string data. Missing this cost a device round trip.
+      memset(new_pages, 0, new_pages_size);
+#else
 #ifndef ABSL_LOW_LEVEL_ALLOC_ASYNC_SIGNAL_SAFE_MISSING
       if ((arena->flags & LowLevelAlloc::kAsyncSignalSafe) != 0) {
         new_pages = base_internal::DirectMmap(nullptr, new_pages_size,
@@ -598,6 +630,7 @@ static void *DoAllocWithArena(size_t request, LowLevelAlloc::Arena *arena) {
       if (new_pages == MAP_FAILED) {
         ABSL_RAW_LOG(FATAL, "mmap error: %d", errno);
       }
+#endif  // defined(__vita__)
 
 #ifdef __linux__
 #if defined(PR_SET_VMA) && defined(PR_SET_VMA_ANON_NAME)
diff --git a/absl/base/internal/low_level_alloc.h b/absl/base/internal/low_level_alloc.h
index 23218dd..77e1137 100644
--- a/absl/base/internal/low_level_alloc.h
+++ b/absl/base/internal/low_level_alloc.h
@@ -36,10 +36,25 @@
 // LowLevelAlloc.
 #ifdef ABSL_LOW_LEVEL_ALLOC_MISSING
 #error ABSL_LOW_LEVEL_ALLOC_MISSING cannot be directly set
-#elif !defined(ABSL_HAVE_MMAP) && !defined(_WIN32)
+#elif !defined(ABSL_HAVE_MMAP) && !defined(_WIN32) && !defined(__vita__)
 #define ABSL_LOW_LEVEL_ALLOC_MISSING 1
 #endif
 
+// __vita__ is excluded above rather than added to ABSL_HAVE_MMAP, which would
+// have been the shorter edit and a false one: there genuinely is no mmap here.
+//
+// What LowLevelAlloc needs is not mmap specifically but page-granularity
+// allocation that does not go through malloc, for two reasons -- so a malloc
+// hook cannot recurse into it, and so it is async-signal-safe. **Neither
+// applies on this platform.** Nothing in this port hooks malloc, and there are
+// no POSIX signals at all, so the guarantee is vacuous and memalign is an
+// honest substitute. See the __vita__ branches in low_level_alloc.cc.
+//
+// Without this, abseil compiles low_level_alloc.cc and create_thread_identity.cc
+// to *empty objects* -- nm reports zero defined symbols -- while absl::Mutex
+// goes on referencing them, which is how this arrived: seven undefined symbols
+// and two object files containing nothing.
+
 // Using LowLevelAlloc with kAsyncSignalSafe isn't supported on Windows or
 // asm.js / WebAssembly.
 // See https://kripken.github.io/emscripten-site/docs/porting/pthreads.html
diff --git a/absl/debugging/internal/elf_mem_image.h b/absl/debugging/internal/elf_mem_image.h
index c8a1036..1ab87f3 100644
--- a/absl/debugging/internal/elf_mem_image.h
+++ b/absl/debugging/internal/elf_mem_image.h
@@ -32,10 +32,14 @@
 #error ABSL_HAVE_ELF_MEM_IMAGE cannot be directly set
 #endif
 
+// __vita__ is in this exclusion list for the reason the comment above gives:
+// the file needs glibc's ELF symbol extensions. VitaSDK's newlib has no
+// <link.h> and no dl_iterate_phdr, and homebrew binaries are statically linked
+// with nothing to introspect anyway.
 #if defined(__ELF__) && !defined(__OpenBSD__) && !defined(__QNX__) &&    \
     !defined(__asmjs__) && !defined(__wasm__) && !defined(__HAIKU__) &&  \
     !defined(__sun) && !defined(__VXWORKS__) && !defined(__hexagon__) && \
-    !defined(__XTENSA__)
+    !defined(__XTENSA__) && !defined(__vita__)
 #define ABSL_HAVE_ELF_MEM_IMAGE 1
 #endif
 
diff --git a/absl/time/internal/cctz/src/time_zone_libc.cc b/absl/time/internal/cctz/src/time_zone_libc.cc
index 362da1a..6262d37 100644
--- a/absl/time/internal/cctz/src/time_zone_libc.cc
+++ b/absl/time/internal/cctz/src/time_zone_libc.cc
@@ -61,8 +61,15 @@ auto tm_zone(const std::tm& tm) -> decltype(tzname[0]) {
   return tzname[is_dst];
 }
 #elif defined(__native_client__) || defined(__myriad2__) || \
-    defined(__EMSCRIPTEN__)
+    defined(__EMSCRIPTEN__) || defined(__vita__)
 // Uses the globals: '_timezone' and 'tzname'.
+//
+// The Vita belongs in this branch and not the default one below: newlib only
+// gives struct tm the tm_gmtoff and tm_zone extension fields when __TM_GMTOFF
+// and __TM_ZONE are defined, and VitaSDK's build defines neither. It does
+// export _timezone and _tzname, which is exactly what this branch wants. The
+// DST offset is therefore assumed to be one hour, as it is for the platforms
+// already listed here.
 auto tm_gmtoff(const std::tm& tm) -> decltype(_timezone + 0) {
   const bool is_dst = tm.tm_isdst > 0;
   return _timezone + (is_dst ? 60 * 60 : 0);
